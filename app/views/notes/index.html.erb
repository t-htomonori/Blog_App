<!DOCTYPE html>
<html>
<head>
  <title>質問</title>
  <meta charset="utf-8">
  <link rel="stylesheet" type="text/css" href="/assets/notes.scss">
</head>
<body>
<div class="question">
<!--
  <p id="notice"><%= notice %></p>
-->
<h1>〜みんなのQ＆A〜</h1>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr　class="notes">
        <td><%= note.title %></td>
        <td><%= note.content %></td>
        <td><%= link_to '投稿を見る', note, class: "btn index-btn" %></td>
        <td><%= link_to '削除', note, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn index-btn" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to '新規投稿', new_note_path, class: "btn newpost-btn" %>

</div>
</body>
</html>